<!-- this is a partial file named 'form' -->
<!-- based on the model 'idea', create a form using the variable 'f' -->
<%= form_with(model: idea, local: true) do |f| %>
  <% if idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
      <% idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, id: :idea_name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, id: :idea_description %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.text_field :picture, id: :idea_picture %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
